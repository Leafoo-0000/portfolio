# Next Steps

1. Update `script.js` to support `year`, `term`, and optional `featured` metadata.
2. Rewrite `index.html` into a clean homepage with featured content only.
3. Create `archive.html` for the year-and-term organizer and move the add item form there.
4. Rework `catalogue.html` so it points to or mirrors the new archive page.
5. Refresh `style.css` for the homepage and archive layout.
6. Validate the homepage, archive grouping, add form, and browser persistence.
